import 'package:flutter/material.dart';

class ListTablesScreen extends StatelessWidget {
  const ListTablesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
